name: test
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: sudo add-apt-repository universe && sudo apt-get update && sudo apt-get install -y build-essential cmake b3sum librust-blake3-dev cmake pkg-config
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure CMake
        run: cmake -B build
      - name: Build project
        run: cmake --build build --config Release
      - name: Run tests
        working-directory: ./build
        run: ctest -C Release
